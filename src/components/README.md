# components

Reusable React UI components for the ERP. Feature-specific components live directly under this folder; primitive design-system components (buttons, inputs, dialogs, etc.) — including any shadcn/ui components added later — live under `ui/`. Components here should be presentational; data fetching and business logic belong in Server Components or Server Actions.
