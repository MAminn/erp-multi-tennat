# prisma

The canonical Prisma schema lives at `prisma/schema.prisma` (created in Task 3) and migrations under `prisma/migrations/`. This folder may also hold seed scripts and schema notes. Never edit generated migration SQL by hand once it has been applied — create a new migration instead.
